# Kbuild external-module build output (see Makefile) -- these are
# regenerated by `make`, never hand-edited, and specific to whatever
# kernel tree they were last built against.
*.o
*.o.cmd
*.ko
*.ko.cmd
*.mod
*.mod.c
*.mod.o
*.mod.cmd
.*.cmd
Module.symvers
Module.symvers.cmd
modules.order
.tmp_versions/
.cache.mk

# Editor/OS cruft
.DS_Store
